architecture:

frontend:
nextjs app

backend:
nodejs (nextjs api routes + server actions)

agent architecture:
- markdown directory of data (memory, reflections, planning) instead of json
- use openai (gpt 5.4 mini => balance cost and effectiveness, $0.75/$4.5 per M tokens)
- 
